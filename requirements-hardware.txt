# Structured-light and real-hardware extras verified in the current project venv.
# Install explicitly when provisioning hardware; no hardware is opened by installation.
-r requirements.txt
open3d==0.18.0
matplotlib==3.10.9
pyorbbecsdk2==2.1.1
